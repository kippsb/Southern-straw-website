# Resend (https://resend.com). Optional in development.
# If RESEND_API_KEY is unset, /api/quote logs submissions to the server
# console instead of sending an email, so the quote form is fully demoable
# before an email provider is configured.
RESEND_API_KEY=
QUOTE_TO_EMAIL=agro3559@gmail.com
QUOTE_FROM_EMAIL=quotes@strawsouthern.com
